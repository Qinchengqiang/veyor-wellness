{
  "name": "json-mock-api",
  "version": "1.0.0",
  "description": "JSON mock API for booking system",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Steven",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.16.3"
  }
}
